spring:
  cloud:
    config:
      discovery:
        enabled: true
grpc:
  port: 6565
eureka:
  instance:
    nonSecurePort: ${grpc.port}
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/